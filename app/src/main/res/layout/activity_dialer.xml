<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_dark"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Top bar: status + logout -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingTop="12dp"
        android:paddingBottom="12dp">

        <View
            android:id="@+id/statusDot"
            android:layout_width="10dp"
            android:layout_height="10dp"
            android:background="@drawable/bg_status_dot"
            android:backgroundTint="@color/status_pending" />

        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            tools:text="Conectado" />

        <ImageView
            android:id="@+id/btnLogout"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_logout"
            android:padding="8dp"
            android:contentDescription="Cerrar sesion"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:tint="@color/text_secondary" />
    </LinearLayout>

    <!-- Number display -->
    <EditText
        android:id="@+id/etNumber"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="32sp"
        android:textColor="@color/text_primary"
        android:gravity="center"
        android:background="@android:color/transparent"
        android:inputType="phone"
        android:cursorVisible="true"
        android:layout_marginTop="24dp"
        android:layout_marginBottom="16dp" />

    <!-- Dial pad -->
    <GridLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:columnCount="3"
        android:rowCount="4"
        android:layout_marginHorizontal="24dp"
        android:useDefaultMargins="true">

        <Button android:id="@+id/key1" style="@style/DialKey" android:text="1" />
        <Button android:id="@+id/key2" style="@style/DialKey" android:text="2" />
        <Button android:id="@+id/key3" style="@style/DialKey" android:text="3" />
        <Button android:id="@+id/key4" style="@style/DialKey" android:text="4" />
        <Button android:id="@+id/key5" style="@style/DialKey" android:text="5" />
        <Button android:id="@+id/key6" style="@style/DialKey" android:text="6" />
        <Button android:id="@+id/key7" style="@style/DialKey" android:text="7" />
        <Button android:id="@+id/key8" style="@style/DialKey" android:text="8" />
        <Button android:id="@+id/key9" style="@style/DialKey" android:text="9" />
        <Button android:id="@+id/keyStar" style="@style/DialKey" android:text="*" />
        <Button android:id="@+id/key0" style="@style/DialKey" android:text="0" />
        <Button android:id="@+id/keyHash" style="@style/DialKey" android:text="#" />
    </GridLayout>

    <!-- Call + backspace row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginVertical="20dp"
        android:layout_marginHorizontal="24dp">

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <ImageView
            android:id="@+id/btnCall"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:src="@drawable/ic_call"
            android:background="@drawable/bg_call_button"
            android:padding="22dp"
            android:contentDescription="Llamar"
            app:tint="@color/white" />

        <View
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <ImageView
            android:id="@+id/btnBack"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:src="@drawable/ic_backspace"
            android:padding="16dp"
            android:contentDescription="Borrar"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:tint="@color/text_secondary" />
    </LinearLayout>

</LinearLayout>
